<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <ListBox x:Name="view1" ItemsSource="{Binding}" Margin="10" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
        <ListBox.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
        <ListBox.ItemTemplate>
            <DataTemplate>
                <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="8" Padding="8" Margin="5" Width="250">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="{Binding FavAnimal.ImageFile}" Width="80" Height="80" Stretch="UniformToFill" Margin="5"/>
                        <StackPanel Margin="10,0,0,0">
                            <TextBlock Text="{Binding FName}" FontWeight="Bold" FontSize="16"/>
                            <TextBlock Text="{Binding LName}" FontSize="14"/>
                            <TextBlock Text="{Binding FavAnimal.Name}" Foreground="DarkGreen" FontSize="14"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>



</Window>
