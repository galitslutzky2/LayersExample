<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
   
        <Grid>
            <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!--<Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
     
        </Grid.ColumnDefinitions>-->
    <ListBox Grid.Row="0" x:Name="view1" ItemsSource="{Binding}" Margin="10" 
            ScrollViewer.HorizontalScrollBarVisibility="Auto">
        <ListBox.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
        <ListBox.ItemTemplate>
            <DataTemplate>
                <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="8" Padding="8" Margin="5" Width="250">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="{Binding FavAnimal.ImageFile}" Width="80" Height="80" Stretch="UniformToFill" Margin="5"/>
                        <StackPanel Margin="10,0,0,0">
                            <TextBlock Text="{Binding FName}" FontWeight="Bold" FontSize="16"/>
                            <TextBlock Text="{Binding LName}" FontSize="14"/>
                            <TextBlock Text="{Binding FavAnimal.Name}" Foreground="DarkGreen" FontSize="14"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
        <StackPanel  Grid.Row="1" Orientation="Horizontal">
        <Button  x:Name="load" Content="Load Data" Width="100" Height="30" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10" Grid.Row="1" Click="LoadData_Click"/>
            <Button  x:Name="add" Content="add Data" Width="100" Height="30" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10" Grid.Row="1" Click="AddData_Click"/>
            <Button  x:Name="update" Content="update Data" Width="100" Height="30" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10" Grid.Row="1" Click="UpdateData_Click"/>
            <Button  x:Name="delete" Content="delete Data" Width="100" Height="30" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10" Grid.Row="1" Click="DelData_Click"/>
            <Button  x:Name="loadAnimals" Content="load Animals" Width="100" Height="30" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10" Grid.Row="1" Click="loadAnimals_Click"/> 
        </StackPanel>

    </Grid>
</Window>
